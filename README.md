# BETH — игровой Telegram‑бот

BETH — это Telegram‑бот, в котором пользователь собирает «Бэтов» (питомцев), зарабатывает внетруеигровую валюту (нейроны) и улучшает уровень своих питомцев через механику ношения.

Бот реализован на `aiogram 3` и использует асинхронный доступ к базе данных через `SQLAlchemy 2` и PostgreSQL.

## Основные возможности

- Регистрация и профиль игрока (`/start`, кнопка «👤Профиль»).
- Внутренняя валюта — нейроны.
- Механика ношения с редкостями, гарантией легендарки и уровнем Бэтов («🤲🏻Ношение»).
- Список всех Бэтов игрока и просмотр характеристик каждого («🐾Мои беты»).
- Технические/админские команды для обслуживания и выдачи бонусов.

## Технологии

- Python 3.11.7
- [aiogram](https://github.com/aiogram/aiogram) 3
- SQLAlchemy 2 (async)
- PostgreSQL + драйвер `asyncpg`

## Установка и запуск

1. **Клонировать репозиторий** (или скопировать проект на сервер/локальную машину).
2. **Создать виртуальное окружение** (рекомендуется):
   ```bash
   python -m venv .venv
   source .venv/bin/activate  # macOS / Linux
   ```
3. **Установить зависимости** (обязательно):
   ```bash
   pip install -r requirements.txt
   ```
4. **Создать базу данных PostgreSQL** и пользователя с правами на эту БД.
5. **Заполнить файл `.env` в корне проекта**:
   ```env
   TOKEN=ВАШ_TELEGRAM_BOT_TOKEN
   DATABASE_URL=postgresql+asyncpg://postgres:пароль@localhost:5432/beth
   ```
6. **Запустить бота**:
   ```bash
   python -m main
   ```

При первом запуске будут созданы все необходимые таблицы в базе данных.

## Конфигурация

Файл `bot/core/config.py` читает переменные окружения из `.env`:

- `TOKEN` — токен Telegram‑бота от @BotFather.
- `DATABASE_URL` — строка подключения к PostgreSQL в формате:
  `postgresql+asyncpg://user:password@host:port/dbname`.

Если какая‑то из переменных не найдена, приложение падает с понятной ошибкой.

## Игровая механика

### Регистрация и стартовые бонусы

- Команда `/start`:
  - Регистрирует пользователя в таблице `users`.
  - Создаёт связанную запись `Player` (игровой профиль), если её ещё нет.
  - Начисляет **400 нейронов** новому игроку.
  - Создаёт базового Бэта:
    - Имя: **Зуппа**
    - Редкость: **Обычный**
    - Стартовый уровень: **5**
  - Показывает основную клавиатуру с кнопками:
    - «👤Профиль»
    - «🤲🏻Ношение»
    - «🐾Мои беты»

### Профиль

- Кнопка «👤Профиль»:
  - Показывает:
    - Никнейм пользователя
    - Ранг
    - Количество нейронов
    - Количество Бэтов

### Ношение

- Кнопка «🤲🏻Ношение»:
  - Требования:
    - На балансе игрока минимум **140 нейронов**.
    - Соблюдение кулдауна (по умолчанию ~6 минут между ношениями).
  - Если нейронов не хватает — приходит сообщение с требуемым и текущим количеством.
  - Если кулдаун не истёк — сообщение с оставшимся временем ожидания.

При успешном ношении:

- С игрока списывается **140 нейронов**.
- Игрок получает случайную награду **от 10 до 25 нейронов**.
- Определяется редкость Бэта (с учётом гаранта):
  - **Обычный** — 80%
  - **Редкий** — 10%
  - **Эпический** — 8%
  - **Легендарный** — 2%
- Работает система гаранта:
  - Каждые **60 ношений** игрок гарантированно получает легендарного Бэта.
  - Счётчик сбрасывается при выпадении легендарки.

Список текущих Бэтов по редкости:

- **Обычные**: Маршл, Тоша.
- **Редкий**: Эмилия.
- **Эпический**: Аминия.
- **Легендарный**: Поли.

### Уровень Бэтов и дубликаты

- Каждый Бэт имеет параметр **`level`** (уровень).
- Стартовый уровень нового Бэта — **5**.
- При получении **того же самого Бэта** (по имени у одного и того же игрока):
  - Его уровень увеличивается на **+5**.
  - Максимальный уровень — **60** (поверх этого значение не растёт).

После ношения игрок видит сообщение:

- Новый Бэт или усиление уже существующего.
- Имя Бэта, его редкость и текущий уровень.
- Сколько нейронов списано и сколько начислено.
- Итоговый баланс нейронов и общее количество Бэтов.

### Мои Бэты

- Кнопка «🐾Мои беты»:
  - Загружает из БД всех Бэтов игрока.
  - Показывает список с inline‑кнопками:
    - Формат кнопки: `Имя (Редкость) • ур. N`.
  - Нажатие на кнопку открывает карточку конкретного Бэта:
    - Имя
    - Редкость
    - Уровень (из 60)
    - Дата и время получения

## Команды бота

Клиентские команды и кнопки:

- `/start` — регистрация в игре, создание профиля, выдача стартовых нейронов и базового Бэта, показ основной клавиатуры.
- `/about` — краткое описание бота и игры.
- `/news` — информация о будущих возможностях и обновлениях.
- Кнопка «👤Профиль» — просмотр профиля игрока.
- Кнопка «🤲🏻Ношение» — запуск ношения.
- Кнопка «🐾Мои беты» — список Бэтов и выбор конкретного для просмотра.

Админские/технические команды:

- `/clear` — попытка очистить последние ~100 сообщений в чате (по умолчанию доступна всем, при необходимости следует добавить проверку прав администратора).
- `/09124467_neurons` — выдать текущему пользователю **1000 нейронов** (скрытый бонус/служебная команда).

## Структура проекта

Основные директории и файлы:

- `main.py` — точка входа, запуск бота и регистрация роутеров.
- `bot/core/config.py` — загрузка конфигурации (TOKEN, DATABASE_URL).
- `bot/core/loader.py` — создание объектов `Bot` и `Dispatcher`.
- `bot/database/models/` — модели БД:
  - `user.py` — пользователь Telegram.
  - `players/player.py` — игровой профиль (`Player`).
  - `bets/bet.py` — модель Бэта (`Bet`).
  - `bets/enums.py` — перечисления редкостей и кодов Бэтов.
  - `bets/poly.py` — конфигурация Бэта Поли.
- `bot/database/requests.py` — регистрация/обновление пользователя (`set_user`), выдача стартовых бонусов.
- `bot/database/request/player_requests.py` — получение/создание `Player` по `tg_id`.
- `bot/service/noshenie_service.py` — игровая логика ношения, шансы, гарант, уровни Бэтов.
- `bot/service/profile_service.py` — вспомогательная логика для профиля (по мере развития).
- `bot/handlers/client/commands/` — хендлеры клиентских команд и кнопок:
  - `start.py`, `profile.py`, `noshenie.py`, `my_bet.py`, `general.py`.
- `bot/handlers/admin/commands/` — административные хендлеры:
  - `clear.py`.
- `bot/keyboards/keyborad.py` — описание основной клавиатуры и вспомогательных кнопок.

## Дальнейшее развитие

Идеи для расширения бота:

- Добавить больше типов Бэтов и уникальные навыки.
- Реализовать PVP‑механику или совместные испытания.
- Ввести более сложную экономику нейронов и квесты.

README можно дополнять по мере появления новых возможностей в боте.
